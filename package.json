{
  "name": "consts-module-generator",
  "version": "0.0.10",
  "description": "Generates npm package with constants from json config using environment configuration",
  "main": "lib/index.js",
  "typings": "lib/index",
  "dependencies": {
    "typescript": "^1.8.7"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --project ./",
    "push": "npm run build && npm version patch && npm --registry http://registry.jsteam.sletat.ru publish"
  },
  "keywords": [
    "typescript",
    "constants",
    "environment"
  ],
  "author": "stepancar",
  "license": "ISC"
}
